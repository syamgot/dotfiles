# Neovim (nvim) 移行ガイド

モダンな Vim の世界へようこそ！ このガイドでは、なぜ移行したのか、古い設定はどうなったのか、そして新しい強力な環境の使い方について説明します。

## 1. Neovim vs. Vim: 何が違うの？
**Neovim** は、Vim を 21世紀向けに「リファクタリング」したものと考えてください。
- **高速**: 動作が圧倒的に軽く、レスポンスが良いです。
- **非同期処理**: プラグイン（Lintやファイル検索など）がバックグラウンドで動くため、エディタがフリーズしません。
- **Lua 設定**: 古い Vim は `Vimscript`（遅くて難解）を使っていました。Neovim は **Lua** という非常に高速な言語で設定を書けます。
- **より良いデフォルト**: マウス操作、シンタックスハイライト、外部変更の自動読み込みなど、現代的な設定が最初から有効です。

## 必須: フォントの設定 (Nerd Fonts)
Neo-tree やステータスラインのアイコンを正しく表示するには、**Nerd Fonts**（アイコン対応フォント）が必要です。
文字化け（□や？）している場合は、以下の手順でフォントを導入してください。

1.  **インストール** (Homebrew経由が簡単です):
    ```bash
    brew install --cask font-hack-nerd-font
    ```
2.  **ターミナルの設定**:
    - ターミナルの設定画面（iTerm2やTerminal.app）を開きます。
    - フォント設定で `Hack Nerd Font` を選択してください。

## 2. 古い `.vim` フォルダはどうなった？
古い `.vim` フォルダには、8〜10年以上更新されていないプラグイン（`NeoBundle`, `neocomplcache` など）が含まれていました。これらが動作の重さや不具合の原因でした。
- **計画**: `~/.config/nvim/init.lua` に全く新しいモダンな設定を作成しました。
- **復元について**: `.vim` を復元されたとのことですので、従来の `vim` コマンドを叩けば、古い環境もそのまま使えます。しかし、**`nvim` コマンドは `.vim` を完全に無視します**。`~/.config/nvim` だけを見に行きます。
- **推奨**: `.vim` はバックアップとして残しておいても良いですが、徐々に `nvim` に完全移行することをお勧めします。

## 3. 新しい環境（機能とキーバインド）

パッケージマネージャとして、爆速の **lazy.nvim** を採用しました。

### 🔍 ファイル検索 (`Telescope`)
`unite.vim` の代わりに `Telescope` を使います。ファイル、テキスト、バッファを一瞬で検索できます。

| キー | 動作 | 覚え方 |
| :--- | :--- | :--- |
| **`<Space>sf`** | ファイル検索 (**S**earch **F**iles) | ファイル名で検索 |
| **`<Space>sg`** | 文字列検索 (**S**earch **G**rep) | 全ファイルの中身を検索 |
| **`<Space>sb`** | バッファ検索 (**S**earch **B**uffers) | 開いているファイルを切り替え |
| **`<Space>sh`** | ヘルプ検索 (**S**earch **H**elp) | ヘルプタグを検索 |

*(注: `<Space>` キーが新しい「Leader」キーです。デフォルトのキーよりも親指で押しやすく便利です)*

### 📂 ファイルエクスプローラ (`Neo-tree`)
ディレクトリツリーを表示して、視覚的にファイルを管理できます。

| キー | 動作 |
| :--- | :--- |
| **`<Space>e`** | ツリーの表示/非表示 (Toggle **E**xplorer) |
| **`a`** | ファイル/ディレクトリの作成 (**A**dd) |
| **`d`** | 削除 (**D**elete) |
| **`r`** | リネーム (**R**ename) |
| **`H`** | 隠しファイルの表示切替 (**H**idden) |

### 🧠 高度な自動補完 (`nvim-cmp` + `LSP`)
`neocomplcache` の代わりに、LSP (Language Server Protocol) を使用します。VSCode レベルの賢い補完が効きます。
- **自動補完**: 入力するだけで自動的に候補が出ます。
- **確定**: `<Enter>` で候補を確定します。
- **ドキュメントスクロール**: `<Ctrl-f>` / `<Ctrl-b>`。

### 🎨 シンタックスハイライト (`Treesitter`)
`Treesitter` をインストールしました。正規表現ではなく、コードの構造を理解して色付けを行います。キーワード、関数、エラーなどの表示が格段に見やすくなります。

### ⚡ プラグイン管理
プラグインの状態確認、更新、デバッグを行うには、以下のコマンドを実行します:
`:Lazy`

## 4. 移行のステップ
1.  **使ってみる**: ターミナルで `nvim` と打って起動します（いずれ `vim` コマンドのエイリアスにしても良いでしょう）。
2.  **プラグインインストール**: 初回起動時、自動的にプラグインのインストール画面が開きます。終わるまで待ちます。
3.  **状態チェック**: `:checkhealth` コマンドを実行すると、環境に問題がないか診断できます。

## 5. よくある質問 (FAQ)
**Q: 今までのキーバインドは使えますか？**
A: はい！ `~/.config/nvim/init.lua` で設定を確認できます。基本的な Vim の操作（`hjkl`, `w`, `b`, `ciw` など）は全く同じまま使えます。

**Q: 設定ファイルはどこ？**
A: `~/dotfiles/dot_config/nvim/init.lua` です。新しいプラグインを入れたり設定を変えたい場合は、このファイルを編集して、`chezmoi apply` してください。
